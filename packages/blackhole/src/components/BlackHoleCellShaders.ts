// The optional cell mode's second raymarch entrypoint stays off the default path.

export { default as bloom } from "../embedded/shaders_webgpu_bloom.wgsl";
export { default as trace } from "../embedded/shaders_webgpu_cell.wgsl";
